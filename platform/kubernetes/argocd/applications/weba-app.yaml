apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: appweba
  namespace: argocd

spec:
  project: default
  source:
    repoURL: 'https://github.com/antonioflaviosf/eng_lab.git'
    targetRevision: main
    path: platform/kubernetes/apps/appweba
  
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: appweba
  syncPolicy:
    automated:
      prune: true
      selfHeal: true