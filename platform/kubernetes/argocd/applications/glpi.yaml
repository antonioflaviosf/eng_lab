apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: glpi
  namespace: argocd

spec:
  project: default
  source:
    repoURL: 'https://github.com/antonioflaviosf/eng_lab.git'
    targetRevision: main
    path: platform/kubernetes/apps/glpi
  
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: glpiapp
  syncPolicy:
    automated:
      prune: true
      selfHeal: true